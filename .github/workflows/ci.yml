name: CI Pipeline

on:
  push:
    branches:
        - main

jobs:
  test-our-code:
    runs-on: ubuntu-latest

    steps:
      - name: Upload code on server
        uses: actions/checkout@v4
      
      - name: Installing Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"